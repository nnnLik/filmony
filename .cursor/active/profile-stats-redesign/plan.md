# План: profile-stats-redesign

## Задача 1 (готово в текущей итерации)

1. Добавить в `ProfileStatsPanel` локальное состояние подвкладки: `Обзор` | `Вкусы` | `Рейтинги`.
2. Визуал: три кнопки в «пилле», как основной таб-бар профиля (карусель Telegram UI для табов при необходимости не требуется).
3. Распределить существующие секции по подвкладкам без удаления блоков данных.
4. Сбрасывать активную подвкладку на «Обзор» при смене `userId` (общий профиль / публичный профиль).
5. Убрать горизонтальный скролл у столбикового графика: колонки в flex с `flex-1`/`min-w-0` под ширину экрана.
6. Проверка: `frontend`: `npm run lint`, `npm run build`.

## Следующие задачи (вне текущего slice или follow-up)

- Опционально: deep-link/query для подвкладки статистики.
- Визуальные улучшения (анимации, плотность, сокращённый дубликат распределения оценок на «Обзор» vs полный на «Вкусы», если понадобится).
- Экспорт в `docs/features/profile-stats-redesign.md` после завершения всего UX-цикла при необходимости.
