# Plan: pet-project-micro-fun

See attached implementation plan (empty states, placeholder, scroll secret).

Implementation: shared `frontend/src/lib/microFun/`, `PlayfulHint`, `EmptyState` props, `MicroFunToast`, `useFeedScrollDepthSecret`.
