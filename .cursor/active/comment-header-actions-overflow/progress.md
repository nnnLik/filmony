## Status

complete

## Completed

- Added `CommentHeaderActions` (Reply icon + ⋯ overflow for Edit / Delete / To-feed); owner action labels inlined (removed `CommentOwnerActionLinks.tsx`).
- Wired `MovieCardDetailPage` and `FeedPostDetailPage`.
- Updated `FeedCard`, `FeedPostCard`, and `TasteQuizCommentAuthorBadge` (hide taste % on own comments).
- Created feature-delivery artifacts and action-log entries.
- Verified: `cd frontend && npm run lint && npm run build` (exit 0).
- Marked feature `done` in registry and published docs.
