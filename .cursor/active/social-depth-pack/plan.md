# Social Depth Pack — implementation plan

See attached plan: slices A→B→C→D.

1. **A** — ListWatchlistOverlapsService + UI
2. **B** — watch_session + CreateCoViewFeedPost + Celery finalize
3. **C** — weekly_controversy_state + digest task + chip
4. **D** — BatchUserRatingStreaksService + RatingStreakBadge everywhere
