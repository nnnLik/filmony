# План: celery-redis-workers

1. Compose + `vars/.env.development` / `.env.example`.
2. Зависимости: `pyproject.toml` / `uv.lock`.
3. `CelerySettings`, `celery_app.py`.
4. `conftest.py` — broker URL для pytest.
5. Документация: `docs/features/celery-redis-workers.md`, `README`, `.cursor/tech.md`.
