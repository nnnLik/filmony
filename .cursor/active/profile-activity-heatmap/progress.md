# Profile Activity Heatmap — Progress

**Status:** done

- Backend migration `y3z4a5b6c789` adds `user_card.completed_at`.
- Stats API extended with `activity_distribution`, `activity_start`, `activity_end`, optional `activity_category_id`.
- Cards list API accepts `completed_on` for drill-down.
- Frontend heatmap component integrated in `ProfileStatsPanel`.
- Tests and docs completed.
