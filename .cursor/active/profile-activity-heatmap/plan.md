# Profile Activity Heatmap — Plan

See `.cursor/plans/github-style-activity-heatmap_90431fba.plan.md` (reference; do not edit).

## Steps

1. Backend: `completed_at` column + stats `activity_distribution` + list `completed_on` filter.
2. Frontend: `ProfileActivityHeatmap` in `ProfileStatsPanel`.
3. URL/query: `completedOn` in rated-cards filters.
4. Tests + docs.
