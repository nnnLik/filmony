# Feature Result Template

## Feature
- Slug:
- Final status: done | in_progress | cancelled

## Implemented
- What was implemented:

## Changed Files
- `path/to/file1` - short reason
- `path/to/file2` - short reason

## Verification
- Commands/checks executed:
- Results:

## Automated tests (backend)
- When `backend/` changed: list new/updated test modules under `backend/src/tests/` and confirm they cover **all** new or modified API and service behavior for this feature (including async paths, errors, and regressions). Note how you ran them (prefer `make backend-test` or `make backend-test-one target=…` from the repo root with compose up).
- If no backend changes: write `N/A`.

## Known Limitations
- Limitation 1

## Next Steps
- Next step 1
