# actor-cast-profile-stats — progress

**Status:** complete

## 2026-08-08

- Created feature branch `feature/actor-cast-profile-stats` from `origin/master`
- Added feature scaffolding
- Models + migration (`person`, `film_actor`)
- `EnsureFilmCastService` + unit tests; card hooks + integration tests
- Backfill command + tests
- Profile stats actor distribution + `actor_kinopoisk_id` filter
- Actors API + integration tests
- Frontend: `ActorDetailPage`, stats donut/insights, rated filter
- Fixed integration test seeds (full `UserCard` fields)
- Closeout: result.md, docs, action-log, HOT; pushed feature branch
