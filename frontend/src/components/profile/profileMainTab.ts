/**
 * Profile is navigated with a single {@link ProfileSectionTabs} control; this type
 * still describes which top-level panel is mounted (movie lists vs statistics).
 */
export type ProfileMainTab = 'movies' | 'stats'
